#ifndef INTERNAL_H
#define INTERNAL_H

/// Global heap metadata.
typedef struct {

} metadata_t;

/// Metadata stored in a heap VMA
typedef struct {

} heap_info_t;

/// Header of a chunk.
typedef struct {

} chunk_header_t;

#endif // INTERNAL_H
